<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:MauiMainPageLoadedTest.ViewModels"
             x:Class="MauiMainPageLoadedTest.MainPage"
             x:Name="Main"
             x:DataType="viewmodels:MainPageViewModel" >

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior 
            EventName="Loaded" 
            Command="{Binding MainPageLoadedCommand}"
            BindingContext="{Binding Source={x:Reference Main}, Path=BindingContext}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <Style TargetType="RadioButton" x:Key="TrackRBStyle">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="IsChecked" Value="False" />
            <Setter Property="IsVisible" Value="True" />
            <Setter Property="TextColor" Value="{AppThemeBinding Light=Black, Dark=White}" />
            <Setter Property="BorderColor" Value="Transparent" />
            <Setter Property="BorderWidth" Value="0" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="HorizontalOptions" Value="Center" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="HeightRequest" Value="32" />
            <Setter Property="CornerRadius" Value="7" />
        </Style>
    </ContentPage.Resources>
    
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <RadioButton
                x:Name="TrackDegFRBtn"
                Content="DegF"
                IsChecked="{Binding DegFChecked}"
                Style="{StaticResource TrackRBStyle}">
                        <RadioButton.Behaviors>
                            <toolkit:EventToCommandBehavior
                                Command="{Binding ChangeTrackDataCommand}"
                                CommandParameter="DegF"
                                EventName="CheckedChanged"
                                BindingContext="{Binding Source={x:Reference Main}, Path=BindingContext}" />
                        </RadioButton.Behaviors>
            </RadioButton>
            
            <RadioButton
                x:Name="TrackDegCRBtn"
                Content="DegC"
                IsChecked="{Binding DegCChecked}"
                Style="{StaticResource TrackRBStyle}">
                        <RadioButton.Behaviors>
                            <toolkit:EventToCommandBehavior
                                Command="{Binding ChangeTrackDataCommand}"
                                CommandParameter="DegC"
                                EventName="CheckedChanged"
                                BindingContext="{Binding Source={x:Reference Main}, Path=BindingContext}" />
                        </RadioButton.Behaviors>
            </RadioButton>

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
